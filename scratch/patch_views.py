filepath = r"c:\Users\dell\Documents\buyology\billed\views.py"

with open(filepath, "rb") as f:
    content_bytes = f.read()

# Let's define the target exactly as it is in the normalized file
target_bytes = b'response = JsonResponse(bills, safe=False)\r\n        response["Access-Control-Allow-Origin"] = "*"\r\n        return response\r\n\r\n            \r\n        response = JsonResponse({"ok": True, "id": new_id})\r\n        response["Access-Control-Allow-Origin"] = "*"\r\n        return response'

replacement_bytes = b'''response = JsonResponse(bills, safe=False)\r\n        response["Access-Control-Allow-Origin"] = "*"\r\n        return response\r\n\r\n    elif request.method == 'POST':\r\n        try:\r\n            data = json.loads(request.body)\r\n        except Exception:\r\n            return JsonResponse({"error": "Invalid JSON"}, status=400)\r\n\r\n        products = data.get("products", [])\r\n        products_json = json.dumps(products) if products else data.get("products_json", "[]")\r\n\r\n        is_delivery = 1 if data.get("delivery") else 0\r\n        is_jenny = 1 if data.get("jenny") else 0\r\n\r\n        payment_mode = data.get("payment_mode", "Cash")\r\n        platform = data.get("platform", "POS")\r\n\r\n        if is_delivery:\r\n            payment_mode = "Nil"\r\n        if is_jenny:\r\n            payment_mode = "Nil"\r\n            platform = "N/A"\r\n\r\n        query = """\r\n            INSERT INTO bills (\r\n                date, customer_name, brand, model, dta, price,\r\n                payment_mode, mixed_cash, mixed_card, mixed_tabby, mixed_tamara, mixed_bank,\r\n                note, transaction_type, platform, delivery,\r\n                exch_new_brand, exch_new_model, exch_new_dta, exch_new_price,\r\n                exch_old_brand, exch_old_model, exch_old_dta, exch_old_price,\r\n                exch_balance, products_json, source, exch_old_source, jenny\r\n            ) VALUES (\r\n                %s, %s, %s, %s, %s, %s,\r\n                %s, %s, %s, %s, %s, %s,\r\n                %s, %s, %s, %s,\r\n                %s, %s, %s, %s,\r\n                %s, %s, %s, %s,\r\n                %s, %s, %s, %s, %s\r\n            )\r\n        """\r\n        \r\n        params = [\r\n            data.get("date", ""),\r\n            data.get("customer_name", ""),\r\n            data.get("brand", ""),\r\n            data.get("model", ""),\r\n            (data.get("dta") or "").upper(),\r\n            float(data.get("price") or 0),\r\n            payment_mode,\r\n            float(data.get("mixed_cash") or 0),\r\n            float(data.get("mixed_card") or 0),\r\n            float(data.get("mixed_tabby") or 0),\r\n            float(data.get("mixed_tamara") or 0),\r\n            float(data.get("mixed_bank") or 0),\r\n            data.get("note", ""),\r\n            data.get("transaction_type", "Sale"),\r\n            platform,\r\n            is_delivery,\r\n            data.get("exch_new_brand", ""),\r\n            data.get("exch_new_model", ""),\r\n            (data.get("exch_new_dta") or "").upper(),\r\n            float(data.get("exch_new_price") or 0),\r\n            data.get("exch_old_brand", ""),\r\n            data.get("exch_old_model", ""),\r\n            (data.get("exch_old_dta") or "").upper(),\r\n            float(data.get("exch_old_price") or 0),\r\n            float(data.get("exch_balance") or 0),\r\n            products_json,\r\n            data.get("source", "Inventory"),\r\n            data.get("exch_old_source", "Inventory"),\r\n            is_jenny,\r\n        ]\r\n\r\n        with connection.cursor() as cursor:\r\n            cursor.execute(query, params)\r\n            new_id = cursor.lastrowid\r\n            \r\n        response = JsonResponse({"ok": True, "id": new_id})\r\n        response["Access-Control-Allow-Origin"] = "*"\r\n        return response'''

if target_bytes in content_bytes:
    content_bytes = content_bytes.replace(target_bytes, replacement_bytes)
    with open(filepath, "wb") as f:
        f.write(content_bytes)
    print("PATCH SUCCESSFUL (CLEAN CRLF MATCH)")
else:
    print("TARGET STILL NOT FOUND")
